# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**How to read this file.** Each entry is a rule plus the consequence of breaking it. The consequence is there so you can judge a trade-off, not as history — where a rule says "this shipped broken once," treat that as evidence the obvious version is wrong, not as an invitation to re-litigate. The README at the repo root is the architecture spec; treat overlap with this file as the README's job.

**What lives here vs `docs/`.** This file holds only what must be in mind *before* you know which file you're opening: money-path and security invariants, boundaries, repo-wide conventions, and the stop-ship checks. Everything else — the deep "why the obvious version is wrong" reasoning for each subsystem — lives in `docs/`, indexed below. **When you learn a new rule, it goes in the relevant `docs/` file unless it (a) can lose funds or leak a credential, (b) governs where code may go, or (c) applies to edits in files you haven't opened yet.** Only those three go here. Without that test this file grows back.

## Read before you edit

These docs hold the "this shipped broken once" reasoning that is not in the code. **Read the relevant one before editing in its area** — the rules in it are not reconstructible from reading the source.

| Doc | Read before touching |
|---|---|
| [`docs/money-boosts.md`](docs/money-boosts.md) | `lib/v4v/boost.ts`, `components/boost-modal/`, `boost-all-modal.tsx`, `lib/v4v/keysend-lookup.ts`, `lib/boost-sound.ts`, `app/.well-known/keysend/`, and the split arithmetic in `lib/util.ts` (`splitSats`, `payableSplit`, `splitAtPosition`, `splitTrackAndHost`) |
| [`docs/streaming.md`](docs/streaming.md) | `lib/v4v/streaming.ts`, `stream-ledger.ts`, `live-value.ts`, `live-block.ts`, `components/streaming-settings.tsx` |
| [`docs/signers.md`](docs/signers.md) | `lib/nostr/signer.ts`, `amber.ts`, `bunker.ts`, `local-signer.ts`, `google-auth.ts`, `drive-backup.ts`, `backup-crypto.ts`, `components/nostr-auth/` |
| [`docs/nostr.md`](docs/nostr.md) | `lib/nostr/*` (non-signer), `components/*note*`, `components/*feed*`, `live-chat.tsx`, `components/profile-editor.tsx`, `components/favorites-sync-notice.tsx` (listed explicitly — `*note*` does **not** match "notice") |
| [`docs/wallets.md`](docs/wallets.md) | `components/wallet-modal.tsx`, `*-wallet.tsx`, `wallet-balance.tsx`, `lib/v4v/nwc.ts`, `spark.ts`, `webln.ts` |
| [`docs/feeds.md`](docs/feeds.md) | `app/api/*`, `lib/pi.ts`, `lib/feed-xml.ts`, `lib/podcast-meta.ts`, `lib/live-status.ts`, `components/podroll.tsx` |
| [`docs/ui.md`](docs/ui.md) | `components/player.tsx`, `fullscreen-player.tsx`, `lib/track-art.ts`, `lists.tsx`, `chapter*`, `transcript*`, `home-page.tsx`, `episode-detail-view.tsx`, theme/PWA |
| [`docs/storage.md`](docs/storage.md) | Adding or changing any `bmb:*` key |
| [`docs/security.md`](docs/security.md) | `lib/safe-fetch.ts`, `safe-url-attr.ts`, `sanitizeShowNotes`, `app/api/transcript`, `app/api/nostr/site-sign`, `next.config.mjs` |
| [`docs/ops.md`](docs/ops.md) | Google Cloud console, DNS, OAuth consent screen |

## Names

- **`boostmebitch`** — repo, working directory, npm package name, and `APP_NAME` default for the Podcast Index `User-Agent`.
- **"Boost Me Bitch"** — display name in the page header and `<title>`.
- **`BoostMeBitch`** — `app_name` in the boostagram TLV JSON and `client` tag on Nostr notes (CamelCase, no spaces — matches the Helipad-aggregator convention used by Fountain, StableKraft, etc.).

## Commands

```bash
npm install
cp .env.example .env.local       # PI key + secret (Spark rail needs no key)
npm run dev / build / start / lint
```

**No test runner, no formatter.** Checks are `npm run typecheck` (`tsc --noEmit`, strict), `npm run lint` (ESLint 9 flat config in `eslint.config.mjs` — `next/core-web-vitals` + `next/typescript`, `no-explicit-any` off for PI's untyped JSON), and `next build`. Path alias `@/*` → repo root.

**Fourteen `check:*` scripts stand in for the tests this repo doesn't have.** Each guards a function whose silent breakage costs a user something irreversible — treat a failure as a stop:

| Command | Guards | Cost of silent breakage |
| --- | --- | --- |
| `npm run check:spark` (`scripts/check-spark-derivation.mjs`) | `sparkMnemonicFromKey`, `deriveBackupKey` | every derived Spark wallet moves; every Drive backup becomes undecryptable |
| `npm run check:sanitizer` (`scripts/check-sanitizer.mjs`) | `safeUrlAttr` — show-notes URL allowlist | a podcast feed runs JS in the origin holding the NWC spending credential |
| `npm run check:ssrf` (`scripts/check-ssrf-guard.mjs`) | `assertSafeFetchUrl` | feed data points the server at cloud metadata |
| `npm run check:liveblock` (`scripts/check-live-block.mjs`) | `parseLiveBlock` — the Split Kit live-value payload → value block | a live show pays the wrong node, drops an artist, or keysends at an email-shaped address |
| `npm run check:keysend` (`scripts/check-keysend-lookup.mjs`) | `isLnurlOnlyAddress` + the short-circuit in `lookupKeysendTarget` — which of two payment rails each lnaddress leg leaves on | under-match: every `@fountain.fm` boost reverts to a keysend that **succeeds** — Fountain accepts keysend, it just never shows the recipient the TLV boostagram — so the sats land, the modal shows ✓, and nothing on this side can tell that the metadata was discarded. Over-match (a bare `endsWith` suffix test): anyone registering `notfountain.fm` can divert other people's recipients off keysend and strip the inline boostagram |
| `npm run check:lnurl` (`scripts/check-lnurl-comment.mjs`) | `buildLnurlComment` — fitting the BoostBox `rss::payment` descriptor + the typed message into the recipient's `commentAllowed` | on an LNURL leg the comment is the ONLY metadata channel, and the descriptor is the machine-readable half (Fountain authored that spec and parses it). Join-then-slice puts the fragile part first and cuts from the right, so a tight budget shortens the URL into a dead link **and** spends the whole allowance on it — no metadata, no message, payment succeeds, nobody notified |
| `npm run check:npub` (`scripts/check-nostr-txt.mjs`) | `parseFeedNpubs` — the `<podcast:txt purpose="nostr\|npub">` + `<podcast:person npub>` → `p`-tag parser | a boost note `p`-tags a stranger under the site's NIP-05-verified identity, permanently and unsendably (over-accept: an unqualified `<podcast:txt>` holds a platform verification token, not an npub), or the artist the feature exists to reach is silently never tagged (under-accept) |
| `npm run check:stream` (`scripts/check-stream-ledger.mjs`) | `accrue`, `creditFixed`, `clearCreditedRun`, `settlePlan`, `settleBatch`, `accruedSats`, `trackBucket`, and the money constants (rate + per-track defaults and ceilings, settle interval, floor, tick + carry caps, minimum play + credit debounce). **`distribute` is module-private, so it's exercised transitively through `accrue`/`creditFixed` rather than imported** — it's the one name here without a direct pin. | streaming drains a wallet at 60× the chosen rate, silently pays nothing, pays a whole batch to the wrong artist, settles one artist per ten minutes while the rest wait, charges a per-track amount *and* a per-minute rate at once, or pays the same song again every time the page reloads |
| `npm run check:favsync` (`scripts/check-favsync.mjs`) | `parseFavoritesList`, `tagsFromList`, **`mergeFavoritesList`**, **`baselineFrom`**, **`planFavoritesPublish`**, `groupLocalFavorites`, `partitionList`, `entriesFromList`, `identifierKind`, `showId`/`itemId`, `parseShowGuid`/`parseItemGuid`/`bareFeedGuid`/`looksLikeFeedGuid`, plus two composed round trips — **idempotence** (`merge(parse(output)) === output`) and the **store projection** (render → regroup → merge reproduces the wire) | favorites the user made in ANOTHER app are silently deleted from relays with no undo (a shared replaceable event has no partial update), or unfavoriting stops propagating forever, or every item is re-parented to the wrong feed because the tag array got rebuilt from structs, or a group opened only to place a track is read back as an album favorite the user never made, or a URL-shaped item guid emits a `k` tag no relay filter matches, or a removal deletes another app's tracks along with the group naming them |
| `npm run check:lease` (`scripts/check-lease.mjs`) | `createLeasePool` — the refcount that lets every NIP-47 call share ONE WebSocket | Alby's relay (Cloudflare in front) rate-limits connections **opened**, not held — measured at 28 dials in 83 s across three boosts, then `429` with `Retry-After: 600`, after which no payment can be published at all. So the app dials once and every payment leg rides that socket, which means a refcount bug doesn't leak, it closes the connection **underneath a payment in flight**: a double `release` (callers run it from a `finally`), or a `release`/`discard` arriving from a STALE lease after a wallet switch, closing the NEW wallet's socket. Timers are injected so the idle window is exercised deterministically rather than by sleeping |
| `npm run check:nwcerror` (`scripts/check-nwcerror.mjs`) | `mapNwcError` — which of three answers a NIP-47 failure gets, and so whether a leg may be retried | a reply timeout reported as failure shows ✗, the user re-boosts, and **every leg pays again** — observed live against AlbyHub, which pushed "Sent 10 sats" for each leg the modal had marked failed. The inverse costs less but still lies: a publish timeout called indeterminate tells the user not to retry a leg that never sent. The two timeout classes **share a parent and carry no distinguishing name or message**, so matching `Nip47TimeoutError` instead of the leaf silently collapses them — which is why this pins the real `instanceof` dispatch against real SDK instances rather than a pure code→label table |
| `npm run check:feedxml` (`scripts/check-feedxml.mjs`) | `readAttr` — the attribute reader EVERY feed parser goes through, including the one that decides who gets paid | it shipped matching on `\b`, and `-` is a non-word character, so `\baddress` matches inside `x-address` and `String.match` returns the FIRST hit. A feed writing `x-address="03ATTACKER" address="03REALARTIST"` was parsed as the attacker's node — and the substitution is invisible, because every other Podcasting 2.0 client reads `address=` and sees a correct feed. Only this parser is steered. Same trick reaches `data-href` → `href` in show notes and `x-feedUrl` → the album feed a value block is read from |
| `npm run check:vts` (`scripts/check-value-time-split.mjs`) | `splitAtPosition`, `splitTrackAndHost`, `payableSplit`, `splitSats` — which `<podcast:valueTimeSplit>` window covers a second, how a boost inside one divides between artist and show, and who a leg that small can actually pay | each is shared by two callers, which is why they're pinned rather than left inline. `splitAtPosition` is used by the boost modal AND `lib/v4v/streaming.ts`: disagree by one second at a boundary and a boost pays one artist while streaming credits a different one, for the same moment of the same episode, with nothing on screen saying so — and the interval must stay **half-open**, because adjacent tracks abut exactly and an inclusive end hands the boundary second to the **outgoing** artist. `splitTrackAndHost` is shared by both boost modals: two copies is how one feed comes to be paid two ways depending on which button was pressed. `payableSplit` is the seam where `splitSats` returning a legitimate 0 meets `payOne` reporting a 0-sat leg as `ok: true` — separately reasonable, together they render a ✓ and write a boost-log entry for a payment nobody attempted. Reasoning in [`docs/money-boosts.md`](docs/money-boosts.md) |
| `npm run check:readtrust` (`scripts/check-readtrust.mjs`) | `readIsTrustworthy`, `syntheticEoseTimeoutFor`, `acceptsEvent` — the rule that decides whether an EMPTY relay read means "nobody has it" or "nothing answered". **The wiring it can't reach** (does `reached` actually increment on a socket opening) needs a scripted local relay; this pins the arithmetic and the predicate, which is the pure half. | every "don't write over what you couldn't read" guard in the app is downstream of this one boolean, so getting it wrong doesn't degrade, it DELETES — an offline device treating an empty read as real replaces the favorites list, the follow list or the profile with whatever that one device happens to hold. Over-blocking is a regression too: count *listed* relays instead of reached ones and a single dead default leaves every read degraded forever |

**The import-free arrangement is now enforced, not just documented.** `favorites-list.ts`, `read-trust.ts` and `lease.ts` must have no imports at all and `nwc-errors.ts` only bare npm ones, because that is what lets a check script load the shipping module under plain Node. `scripts/import-free.mjs` scans for it and each of those four scripts calls it. The scan rejects **type-only** relative imports too, and that is the whole point: `import type { X } from './y'` is erased by type-stripping and loads fine, so it passes every other check while leaving the module one `type` deletion from breaking — at which point the tempting repair is to stop importing the real module and copy its functions into the script, green forever and guarding nothing. The failure message says so explicitly.

Each **imports the real module** via `node --experimental-strip-types` — a reimplemented copy passes green while shipping code drifts, the exact failure being guarded. Each carries a *must-still-work* half (legitimate URLs, real podcast hosts, valid mnemonics), because over-blocking is a real regression too. **Vectors were generated by the shipping code; if one fails, fix the code — never edit the vector to match.**

**Build a fixture from the WIRE, never from the struct you parse it into, and check a new vector fails against something before you keep it.** A round-trip assertion whose input is constructed out of your own fields cannot fail: you write the positions you know, read them back, and the comparison is vacuously true while the code silently truncates everything else. `check:favsync` shipped exactly that — a vector literally named "lossless", green for the entire life of the feature, while every publish dropped whatever another app had written past position 3. So every fixture in `check-favsync.mjs` is a literal tag array a relay could have sent, several of them lifted verbatim from the live event (`npm run probe:favorites -- <npub> --dump f.json`) — real wire data carries the shapes nobody thinks to invent, like the non-UUID item guid that would have made a UUID-gated item parser look correct forever. The second half matters as much: a vector that passes the moment it is written has proved nothing. When you are replacing an implementation, run it against that one first. When there isn't one — a wholesale format change makes the old exports vanish, so "it fails" is meaningless — run it against the *obvious wrong* implementation instead, which is what `naive()` at the foot of that file is for.

Run all of them alongside typecheck/lint/build before anything ships. **Stop the dev server before `npm run build`** — the build rewrites `.next` and the running server then serves a mismatched chunk manifest.

**And `rm -rf .next` before starting `dev` again, because the collision runs BOTH ways.** Starting the dev server on a `.next` a production build just wrote is equally broken and much harder to read: the dev server inherits production manifests (`prerender-manifest.json` sitting beside `static/development`) and serves a chunk graph that doesn't match. It surfaces as a Next dev overlay reading **`undefined is not an object (evaluating 'originalFactory.call')`** — a React Refresh error that looks like an application bug and sends you hunting through app code for a fault that isn't there. Then note the second half: a phone or second browser **keeps serving those broken chunks from its own cache** after the server is fixed, and a plain reload will not evict them, so retest in a private tab before concluding the fix failed.

`.env.local`: `PODCAST_INDEX_KEY`/`SECRET` (server-only), `APP_NAME` (optional). The Spark rail needs **no** API key.

## Server vs client boundary (don't cross it)

Podcast Index credentials must never reach the browser. Enforced by file conventions, not bundler config.

**Server-only:** `lib/pi.ts` (uses `node:crypto`, reads `process.env`), imported by `app/api/*` and by `app/page.tsx`'s `generateMetadata` — both server-side, so the credential invariant holds, but don't read the rule as "API routes only"; the BoostBox proxy `app/api/lightning/boostbox/route.ts` (reads `BOOSTBOX_URL`/`BOOSTBOX_API_KEY` and forwards); `lib/safe-fetch.ts`; `lib/rate-limit.ts` (per-IP sliding window, first line of every API route).

**Browser-only:** `lib/store.ts`, `lib/v4v/nwc.ts`/`webln.ts`/`lnaddr.ts`/`spark.ts`/`boostbox.ts`, `lib/nostr/`, `lib/storage.ts`, `lib/podcast-meta.ts` — they touch `window.*`, storage, IndexedDB, or WASM. SSR guards exist but assume client context.

**Isomorphic:** `lib/types.ts` (pure types), `lib/v4v/boost.ts` (orchestration).

Components fetch via local API routes (`fetch('/api/feed?id=…')`) — never call PI directly.

## Cross-cutting rules

Each of these has cost something real. The reasoning is in the linked doc; the rule is here because you can break it from a file that looks unrelated.

- **`safeUrlAttr` is a scheme ALLOWLIST and must stay one.** Never "improve" it into a denylist of bad schemes — it shipped that way and six vectors reached the DOM as live `javascript:`. Show notes render via `dangerouslySetInnerHTML` and this origin's `localStorage` holds a spending credential. → [`docs/security.md`](docs/security.md)
- **Every server-side fetch of a feed/chapter/transcript URL goes through `safeFetch(url, init)`, never a bare `fetch`** — it re-validates every redirect hop, AND resolves each hostname, because an IP-literal check alone is beaten by a public DNS record needing no attacker infrastructure (`http://127.0.0.1.nip.io/`). → [`docs/security.md`](docs/security.md)
- **Read a proxied body with `readCappedText`/`readCappedJson`, never bare `res.text()`/`res.json()`.** `AbortSignal.timeout` caps how LONG a fetch runs, not how many bytes it returns, and every proxied URL here comes from feed data. Any new module-level cache of feed content needs a size ceiling and real eviction too — `rssXmlCache` and `feedCache` both grew forever because entries past TTL stopped being *served* but were never *deleted*. → [`docs/security.md`](docs/security.md)
- **Read a feed attribute with `readAttr` (`lib/feed-xml.ts`), never a local regex** — and never relax its `(?:^|\s)` anchor to `\b`. `-` is a non-word character, so `\baddress` matches inside `x-address`, and a decoy attribute ahead of the real one steers the parser to a different payee while every other Podcasting 2.0 client reads the feed correctly. `lib/musicl-resolver.ts` had its own copy and was independently vulnerable. Pinned by `npm run check:feedxml`. → [`docs/security.md`](docs/security.md)
- **A route's unhandled 500 returns its `fallback`, never `e.message`** (`lib/api-handler.ts`). `PiHttpError` carries Podcast Index's raw body and `assertSafeFetchUrl` names the host it rejected, so reflecting the message turns the SSRF guard into an oracle. Deliberate 400/502 messages a route writes itself are fine — they never reach that catch. → [`docs/security.md`](docs/security.md)
- **`nostr-tools` is pinned to exact `2.19.4` — do NOT bump it or relax the caret.** `2.20.0+` breaks `nostrconnect://` login. `npm update` reintroduces it. → [`docs/signers.md`](docs/signers.md)
- **Publish a signer's `nostrApi`, never the signer instance.** `private sk` has no runtime effect, so assigning the instance exposes the raw secret key as `window.nostr.sk` to every script on the origin. Don't add a key-export method to `LocalSigner`, and don't add a module-level accessor that returns the key. **The prohibition is on *ambient* reach, not on the user seeing their own key** — `<ExportKeySection>` reveals the nsec into component state on an explicit click, gated on `storage.signer.get() === 'local'`, via the same `getKey()` that `provision-spark.ts` already uses. That carve-out is the only one; widening it to anything a script can reach without a person asking is the bug this rule exists for. → [`docs/signers.md`](docs/signers.md)
- **Never publish a kind:3 follow list you didn't reliably fetch, and never record it as published without `assertPublished`.** A blind republish wipes the user's whole follow list; an unasserted one promotes a list no relay holds to `storage.follows`'s "last-known-good", which is the cache the degraded-write guard reads. `publishFollow` shipped without the assert while `favorites.ts` and `wallet-backup.ts` both had it. → [`docs/nostr.md`](docs/nostr.md)
- **`publishSignedEvent` resolving is NOT proof anything landed** — it uses `Promise.allSettled` and never rejects, so `acceptedRelays` can be `[]`. Any caller that records durable state on the strength of a publish must gate it on `assertPublished(note, what)` (`lib/nostr/publish.ts`). The favorites baseline shipped without it: a favorite that reached nobody entered the baseline anyway, which makes `adds = local − baseline` empty for that id from then on, so **it was never published again on any subsequent toggle** while the UI reported success. Losing a publish is only recoverable if the next one retries it, and recording success is exactly what stops the retry. Put the assert in the feature's own publish wrapper, not at each call site — three writers is three places to forget. → [`docs/nostr.md`](docs/nostr.md)
- **Favorites are ONE plain replaceable event at `kind:10333`, shared with other apps: read → merge → publish, always, and never on a degraded read.** No `d` tag, so the read passes `dTag: ''` to `acceptsEvent` (which matches an *absent* `d`) or an addressable event sharing the kind gets laundered into the user's favorites. One event with many writers and no partial update means a blind publish deletes whatever StableKraft (or any third app) added — silently, on someone else's device, with no undo. There is deliberately no exported "publish my favorites"; `syncFavorites` is the only writer and it reads first. Under wholesale replacement a degraded read is the most expensive mistake available: one bad read, republished, is the entire list. **The wire format is an external spec and is NOT ours to change** — [github.com/ChadFarrow/PC20-Nostr](https://github.com/ChadFarrow/PC20-Nostr/blob/main/pc20-favorites.md), so neither implementing app owns it; a format change is a PR there before it is a commit here. `docs/pc20-favorites.md` is only a stub pointing at it — don't re-grow a local copy. → [the spec](https://github.com/ChadFarrow/PC20-Nostr/blob/main/pc20-favorites.md)
- **TAG ORDER IS THE DATA — never rebuild the tag array from parsed structs.** An `i` tag is bare (`['i', '<identifier>']`, two elements). An item's parent feed is *the most recently opened `podcast:guid:` group* and its medium is *the last `['medium', …]` tag above it*, both carried by position and by nothing on the entry itself. So the obvious implementation — parse to structs, merge, rebuild — silently reattaches every item to the wrong feed and re-labels everything past a medium boundary, with no error and nothing on screen. The parsed model is therefore an **ordered node list** (`ParsedList.nodes`), the merge is an edit of that list, and `tagsFromList` walks it in place. This is the same mistake the predecessor format made one level down (rebuilding a single `i` tag from its fields, which ate every position past the third for the feature's entire life) with a much bigger blast radius. **A local value never overwrites a non-empty one another writer set** — "prefer my own resolved value" makes two apps rewrite the event against each other forever, each publish locally reasonable, the only symptom being that it never stops. → [`docs/nostr.md`](docs/nostr.md)
- **A feed group is not always a favorite, and the baseline is what tells a foreign entry from one you removed.** Opening a group is the only way to name an item's parent, so a group appears whether or not the feed was favorited — on the live list, 197 groups carry 38 unambiguous favorites. **Only an *itemless* group reads back as a feed favorite**; treating every `podcast:guid:` as one manufactures albums the user never made, every page load. The one exception is a record that predates the ambiguity and is not contradicted by the wire, only unrepresentable in it: this device's own `storage.favorites` cache. (A second exception, a pre-10333 event naming the feed outright, went away with the migration path — `explicitFeeds` is not in the hydrator, whatever the older prose said.) Separately, `storage.favBaseline` (`{feeds, items}` of ids this device last agreed with the relay on) answers the question a *second* writer must answer and a single writer never faces: an entry on the relay and absent locally is either another app's addition or this device's removal. Prefer the relay and unfavoriting stops working; prefer local state and you delete the other app's entries. **A group we published keeps its place while any item under it survives** — dropping it takes the other app's tracks with it, since the group is the only thing naming their parent. → [`docs/nostr.md`](docs/nostr.md)
- **Never offer a favorite whose PARENT FEED can't resolve — and "not resolved yet" is not the same claim as "will never resolve".** A host may point a `<podcast:valueTimeSplit>` at a **publisher** feed, whose `<podcast:remoteItem>` entries name the real album feeds; `resolveRemoteItemFromRss` walks that chain and returns a value block, a title and a cover **either way**, so the row looks fully resolved while its `remoteItem.feedGuid` names something `/episodes/byguid` will never take as a `podcastguid`. Favoriting it writes an entry to the shared kind:10333 list that **no app can ever open**, on every device, with no undo. `ValueTimeSplit.parentFeedGuid` carries the verdict in three states — `undefined` (nothing learned, use the wire value), a string (album guid recovered during the walk, use it instead), `null` (**known unresolvable — render no heart**, the same refusal `<FavEpisodeHeart>` makes for an episode with no parent feed). The obvious test, "suppress when `episodeGuid` is missing", is the trap: that is equally true when PI merely hasn't crawled the item yet, which is the independent release this app exists to pay and whose placeholder self-heals on a later load. Any new surface offering a favorite for a remote item inherits this. → [`docs/nostr.md`](docs/nostr.md)
- **Carry what you can't read.** An identifier kind outside the table, a tag type we have no meaning for, a `k` naming a kind we never emit, a malformed `podcast:guid:` — all belong to a writer newer or older than us and ride through untouched, as `LooseEntry` nodes holding the *whole tag* (a future writer may use NIP-73's third element). A loose node deliberately does **not** close the open group: an unrecognized `i` between a feed and its items must not re-parent the ones after it. "This app can't read it" is not the same claim as "this is junk" — malformed entries are removable only through the explicit `bmbCleanFavorites()` hook. → [`docs/nostr.md`](docs/nostr.md)
- **A guard that silently withholds must say so.** The favorites degraded-read branch keeps local state and publishes nothing, which is right, and it announced nothing, which is why it read as "your favorites are gone" on any device with no cache — and nearly got reverted for causing the bug it was preventing. Any new "we couldn't verify this, so we're doing nothing" path needs a user-visible state, not just a `console.warn`: for favorites that's `favoritesSync` in `lib/store.ts` and `<FavoritesSyncNotice>`. **A silent correct decision is indistinguishable from a broken one**, and the person who can't tell is you, six weeks later, in production. → [`docs/nostr.md`](docs/nostr.md)
- **Same for kind:0 — merge an edit over `fetchRawProfile`, never over `ProfileMetadata`, and never publish an untrustworthy read.** `fetchProfile` returns a seven-field whitelisted subset, so a profile rebuilt from it silently deletes `banner`, `website` and every field another client set. Both writers go through `publishProfile` (`lib/nostr/profile.ts`). → [`docs/nostr.md`](docs/nostr.md)
- **Never record an absence you didn't reliably observe, and don't take the relay library's word for it.** A null from a relay query means "nobody had it" *or* "nothing answered in time" — any new negative cache must tell them apart, and `fetchLatestEventDetailed`'s `trustworthy` is how. **Do not compute that from nostr-tools' aggregate `oneose`**: a failed connection calls `handleClose` → `handleEose`, so offline fires the aggregate in ~19 ms and reports a cleared library as fact. The rule is `event_in_hand or (reached > 0 and answered == reached)`, counted per relay, with never-connected relays *excluded from the denominator* — requiring every listed relay to answer means one dead default degrades every read forever. The library also synthesizes an EOSE on a timer (`baseEoseTimeout`, 4400 ms) that is indistinguishable from a real one at the callback, so any new query passes `syntheticEoseTimeoutFor(maxWait)` to push it past its own deadline. `check:readtrust` pins all three. → [`docs/nostr.md`](docs/nostr.md)
- **A Podcast Index "not found" must reach the client as 404, never 500** — and **PI signals not-found with HTTP `400`**, not 404, so this does not happen by itself. Every new `lib/pi.ts` wrapper needs `catch (e) { if (e instanceof PiHttpError && (e.status === 400 || e.status === 404)) return null; throw e; }`; the route turns that `null` into a 404. Swallow the miss **in the wrapper, not the route** — the route can't tell a miss from an outage once the throw reaches it, and auth/5xx must still propagate. A 5xx trips the client-side breaker and disables *all* metadata resolution for the rest of the tab, and because resolution is **probe-first-then-batch** it doesn't cost you one item, it silently cancels the other N−1: `getEpisodeByGuid` shipped without the catch and a single unindexed music feed took **227 favorites to 0**, with the app reporting no error. `getPodcastByFeedUrl` is the reference implementation. **That symptom had a second, independent cause** — the hydrator built its store maps from PI-resolved entries only, so an outage pruned the store while the baseline still named the full set, and the next page load published the deletion. Fixing one did not fix the other; if favorites vanish after a PI wobble, check both. → [`docs/feeds.md`](docs/feeds.md)
- **A route that fans out to PI must cap the fan-out** and follow probe-first-then-batch — an attacker-chosen list length otherwise turns one request into N parallel PI calls. → [`docs/feeds.md`](docs/feeds.md)
- **The rail picker stays in BOTH boost modals** (`components/rail-picker.tsx`). Letting one pick silently via `pickRail()` was a money bug: a funded Spark wallet sat untouched while boosts went out of an old WebLN extension. → [`docs/wallets.md`](docs/wallets.md)
- **Every `client()` in `lib/v4v/nwc.ts` opens a WebSocket and MUST be closed in a `finally`.** nostr-tools doesn't dedupe relays by URL and `executeNip47Request` closes only its subscription, so an unclosed client strands a half-open socket (`enableReconnect: false`, no ping) against the relay's connection cap. Four of five call sites leaked, and it degraded in two stages inside one session: first `publish` still landed while the reply was starved — **so the wallet paid and every leg reported failure**, which re-boosting then pays twice — and then connections were refused outright and nothing sent at all. A page reload cleared it, which is why it read as random flakiness. The balance chip was the amplifier, not the payments: `useWalletBalance` refreshes on every `payment_sent` push and is mounted twice during a boost, so each paid leg stranded two more. → [`docs/wallets.md`](docs/wallets.md)
- **Feed artwork must never outrank the enclosure, and both art surfaces must share one gate.** Chapter art is arbitrary third-party media, routinely hosted on the **same origin as the audio** and routinely enormous — 20–36 MB animated GIFs per chapter on a real music feed, against a 175 MB mp3 on one HTTP/2 connection. Skipping chapters starved the element until it sat at `readyState 1` with nothing buffered, and because a starved element keeps `paused === false` the transport showed ❚❚ over silence. Measured A/B, six skips over 2 Mbit: images blocked → +13.1 s of playback, images allowed → **+0.0 s**. `<Player>`'s `artOk` gate is the fix; `nowPlayingArt(…)` (`lib/track-art.ts`, gated) is what both the mini-bar and the fullscreen hero render, because a gate on one surface only is no gate at all — and because that one helper also owns the *precedence* (live block → valueTimeSplit track → chapter `img`), so the two surfaces can't disagree about whose art is showing while a boost pressed on either pays the same artist. **`waiting`/`stalled` close it, not the headroom sampler** — that rides on `timeupdate`, which a wedged element stops firing, so a sampler-only gate stays open in precisely the case it exists for. **The mirror image is the expensive half: closing is cheap, reopening is not, and a gate with one way in is a trapdoor.** `timeupdate` as the only sampler meant a gate shut by the `waiting` iOS raises on a seek never reopened while paused (no ticks, and a finished download emits no `progress` either), and `ahead >= 20` was unreachable in the last 20 s of every episode — where a music show puts the closing song. So `progress` and `playing` sample too, the thresholds are `Math.min(threshold, timeLeft)`, and the gate does not apply at all while nothing is playing: with no enclosure in flight there is nothing for artwork to outrank. Any new surface that renders feed-supplied artwork alongside playback goes through the gate. → [`docs/ui.md`](docs/ui.md)
- **New API route starts with `rateLimit(req, '<route>', N)`** and sets `Cache-Control` on **200 responses only**. Returns `{ error }` via `getErrorMessage(e, fallback)`.
- **All `bmb:*` access goes through the typed helpers in `lib/storage.ts`** — never `localStorage.getItem`/`setItem` **or `sessionStorage.*`** anywhere else. The sessionStorage half was the gap: `bmb:nwc_uri_sess:<npub>` holds a spending credential and had its key hand-written in four places, where the disconnect path was one character from no longer matching the writer. New persisted field ⇒ new accessor. → [`docs/storage.md`](docs/storage.md)

## Boost flow invariants

`components/boost-modal/index.tsx` orchestrates; `lib/v4v/boost.ts` is the engine.

0. **Live-stream boosts go out as real NIP-57 zaps** — a branch at the top of `go()`, before the boostagram path, when signed-in + active signer + single lnaddress recipient + host supports NIP-57 (pre-checked via `lnaddrSupportsZaps` BEFORE paying → no double-pay). Uses `sendZap` with `aTag = streamChatAddr(id)`. Falls through to the normal path otherwise. Both paths share `logStoredBoost(legs)` and `maybePublishNote(results)` — extracted so the StoredBoost/publish blocks can't drift. On a real zap the kind:1311 text line is skipped (the receipt already shows); on the fallback it's posted. The modal auto-closes ~1.5 s after success. **The zap carries the BoostBox descriptor too**, via `sendZap`'s optional `metadata` — it POSTs to BoostBox itself and builds the LUD-21 comment with the same `buildLnurlComment` an ordinary LNURL leg uses, because `boostbox.ts` is not on the surface components may reach and a second copy of the desc-plus-message rule in the modal would drift. Without it a live-stream boost to a Fountain host arrived with the typed message and no machine-readable metadata at all — the exact gap the `@fountain.fm` divert exists to close, which never covered this path because zaps don't go through `payLnurl`. **The descriptor goes in the LUD-21 `comment`, never in the kind:9734 `content`**: that content is what every Nostr client renders as the zap message, so a descriptor there prints `rss::payment::boost <url>` in the middle of the user's prose. No `check:*` can pin that — `zap.ts` reaches `../storage` through `./nwc` and won't load under plain Node.
0.5. **A boost pressed inside a `<podcast:valueTimeSplit>` window pays the TRACK, in two legs.** The live path has redirected to the artist on stage since `live-value.ts` shipped; the pre-recorded equivalent did not, so pressing BOOST during a song paid the show and put no `remote_*` guids on the wire — the artist got nothing and the host couldn't tell a song boost from any other. It hid for the whole life of the feature because the live path works by *rewriting `episode.value`*, which the modal happens to read, so there was no path from `episode.valueTimeSplits` into the modal at all. `useActiveSplit` (`components/boost-modal/use-active-split.ts`) resolves it; `splitTrackAndHost` divides it; `sendBoost` runs twice, sequentially — `remotePercentage` to the track's block, the remainder to the show's — both boostagrams carrying the track's `remote_feed_guid`/`remote_item_guid` so Helipad correlates them. Six rules, each one a way to pay the wrong person or lie about having paid:
    - **The target FREEZES at modal open.** The episode keeps playing while the user picks an amount and types, so a live-following target lets a boost aimed at the song land on the show because the song ended during typing — after the user read "paying Copenhagen Time" and pressed the button. The live path follows live only because it has no fixed timeline to freeze against.
    - **`positionSec > 0` is the "is this episode actually playing" test**, and it's how every call site already encodes that: `<Player>` passes the live position, `<EpisodeDetailView>` passes `isThisPlaying ? positionSec : 0`, `<Lists>` passes nothing. Without it a split authored at `startTime: 0` redirects a boost pressed from a list row by someone listening to nothing.
    - **The send button is DISABLED while the split resolves.** The window is known synchronously (`startTime`/`duration` ride on the episode) and the target is not (it needs `/api/value-splits`), so a fast tap in that gap pays the show a moment before the modal promises the artist. This is a money gate, not a spinner.
    - **The show's remainder is PAID, however small, and `payableSplit` decides who can receive it.** 100 sats at 97% is 97 and 3 — that is what the feed asked for. Folding a too-small remainder into the track was the first fix and it was wrong twice over: it moved money to solve a display problem, and it made a 100-sat boost pay the show nothing. The real fault is a collaboration between two individually reasonable behaviours — `splitSats` honestly returns 0 for a payee it can't reach, and `payOne` short-circuits `sats <= 0` to `ok: true` **without contacting anyone** — so a 3-sat leg across 4 payees rendered a ✓ and wrote a `StoredBoost` entry for a payment that never happened. `payableSplit` drops those payees and re-splits over the rest. **One object feeds the preview and the send** (re-deriving the trimmed set at send time would let the approved rows differ from the legs that go out), and the dropped payee is **named on screen** rather than silently absent from a list someone is reading to check where their sats went.
    - **The host leg is non-fatal and never merged into the track block.** The artist's leg has already paid by the time it runs, so a throw would report a partly-successful boost as a total failure and talk the user into paying twice. Merging the two blocks would need both sets of weights rescaled against a common denominator and would break `splitSats`' one-sat floor. An unresolvable remote item falls back to the show's block **and says so on screen** — PI hasn't crawled every album feed, so that's ordinary, and a silent fallback is indistinguishable from the bug this replaced.
    - **`fireConfetti`/`playBoostSound` fire ONCE, after BOTH blocks settle** — not inside the first `sendBoost`, where they started. A redirected boost celebrated when the *track* legs finished, so the confetti popped and the chime played while the show's legs were still going out and the button still read "sending…". A success chime over money that is still moving makes the same false claim a ✗ on an unanswered wallet does, pointed the other way. `primeBoostSound` stays synchronous in the click handler or iOS blocks the unprimed `play()`. **One `StoredBoost` and one note for the whole thing** — the user pressed boost once; `storedBoostLegs` runs per group and the results concatenate track-first, because a merged sort would compare a host payee's raw weight against an artist's across different denominators.

1. **Lightning first, then Nostr.** `publishBoostNote` fires only after `sendBoost` returns *and* `collected.some(r => r.ok)`. Don't reorder — inverting publishes false "I boosted" notes when every payment fails.
2. **`pickRail()` = rail pref first, then NWC > Spark > WebLN.** `storage.railPref` wins when that rail is still connected/enabled; otherwise priority order. The user can override per-boost. `storage.railPref.set/clear` notify `subscribeRailPref` so the balance chip, account-menu summary and open wallet modal re-resolve on a switch.
3. **Episode value-block fallback happens server-side** — `app/api/feed/route.ts` does `e.value ?? podcast.value`. Don't re-implement in the modal.
4. **Splits use weights, not percentages.** `ValueRecipient.split` is a weight; total weight is the denominator. `splitSats()` is **largest-remainder (Hamilton)**: floor every share, then hand the leftover sats out one at a time to whoever was rounded down the most, fee recipients losing ties. Every positive-weight recipient is then guaranteed **at least 1 sat**, funded from the largest allocation so the total is preserved — that's what the 100-sat minimum boost is for. The obvious alternative, floor-everyone-then-dump-the-remainder-on-the-first, is the bug this replaced: a 100-sat boost to a 98%/1%/1% block floors both small legs to 0 and sends the whole 100 to the artist. **It lives in `lib/util.ts`** (with `recipientOrder`, for the same reason: `<SplitsPreview>` renders an allocation and must not import the payment engine to do it) and is re-exported from `lib/v4v/boost.ts`, so call sites and the swap-out boundary are unchanged. **When there are fewer sats than recipients it tops up as many as it can and leaves the rest at 0 — and a 0-sat leg is `ok: true` in `payOne`, i.e. a ✓ for a payment never attempted.** Any caller splitting a DERIVED amount (a `remotePercentage` remainder, which is arbitrarily small by construction) must go through `payableSplit` instead, which drops the unreachable payees and re-splits over the rest.
5. **TLV record `7629169` only.** Boostagram JSON goes there (PC2.0 standard), with `sender_id` inside it. We deliberately do **not** also emit a `696969` sender record — that key collides with shared-node sub-account routing (getalby.com uses `customKey=696969 customValue=<sub-account>`). Per-recipient `customKey`/`customValue` from the value block IS attached to the keysend.
6. **WebLN `customRecords` are plain JSON, not hex.** WebLN providers hex-encode internally; pre-hexing double-encodes and Helipad can't `JSON.parse`. NWC's `pay_keysend` is the opposite — NIP-47 requires hex-encoded TLV. `tlvHexFor` (NWC) vs `recordsForKeysend` (WebLN) look symmetric and are genuinely different wire formats.
7. **Note amount is intent, not actual.** `formatContent` and the `amount` tag use `boostagram.value_msat_total`, not the sum of successful legs. A user who boosts 100 sats with one failed leg still posts "Boosted 100 sats"; the partial breakdown is in the modal and Helipad.
8. **BoostBox is LNURL-only.** `lib/v4v/boostbox.ts` POSTs metadata via `/api/lightning/boostbox` *before* `fetchLnInvoice`, then puts the returned `desc` (`rss::payment::boost <url>`) in the LUD-21 `comment` field. **The desc and the typed message are handed to `fetchLnInvoice` as SEPARATE args, never pre-joined** — `buildLnurlComment` (`lib/util.ts`) keeps the desc whole or drops it, because that descriptor is a spec Fountain authored and parses, and join-then-slice cut the URL rather than the prose. Keysend recipients are untouched (TLV carries the boostagram inline). BoostBox failure is non-fatal; LNURL falls back to `boostagram.message`.
9. **LNURL invoices are amount-verified before paying.** `fetchLnInvoice` and `sendZap` decode the returned BOLT11 via `bolt11AmountMsat` (`lib/v4v/bolt11.ts`, a pure HRP parser shared with the zap-receipt reader) and throw on an amountless invoice or any mismatch — a malicious LNURL server can't substitute a larger one. Strict equality is safe because we only request whole-sat msat values; the throw surfaces as a normal per-leg `{ ok: false, error }`.
10. **Payment order IS display order — `sendBoost` traverses `recipientOrder(recipients)`,** biggest share first, the same helper `<SplitsPreview>` and `<ValueSplitRows>` render by. Feed order is *authoring* order: a live block held the artist at 94.1% (311 of 333 sats) and listed them first in the modal, while four 1-sat housekeeping payees settled with a ✓ ahead of them — the screen said one thing and the wallet did another. It also maximised the failure surface, since legs are sequential and a rail can die mid-boost; descending by weight means a partial failure drops the dust.
    - **It reorders the TRAVERSAL, never the array.** `recipients`, `splits` and `results` stay positionally coupled, so `results[i]` is always `recipients[i]`'s leg. Sorting the array instead would perturb `splitSats`' largest-remainder tie-break by a sat per payee and repoint every positional read downstream. `results` is **pre-sized and assigned by index** — `results.push(r)` type-checks, lints and builds while pairing every leg with the wrong recipient.
    - **`onProgress` reports the recipient's index, not the settle sequence.** Consumers must write `results[index]`; the boost modal's old `[...prev, res]` append was correct only while the two coincided. Its `results` state is therefore `(BoostResult | undefined)[]` — the union is load-bearing, since this repo doesn't enable `noUncheckedIndexedAccess` and a bare `BoostResult[]` would type-check while lying about the holes. `<LightningStatus>` counts what's present rather than reading `results.length`, which is the recipient count from the first render.
    - **Legs stay sequential — never parallelize.** NWC is a single relay connection, WebLN prompts per payment, and `streaming.ts`'s design invariant is that settles are serialized.
11. **A wallet that never answered did NOT fail — never report it as one, and never retry it.** The Alby SDK gives a NIP-47 payment 60 s for a reply (reads get 10 s) and then throws `Nip47ReplyTimeoutError`. That is not a refusal: the request was published and the wallet may have paid. Observed live against AlbyHub — every leg showed ✗ in the modal while Alby pushed "Sent 10 sats" per leg and the recipient's wallet showed the sats two minutes later. `mapNwcError` turns it into a typed **`NwcIndeterminateError`**, `payOne` sets `BoostResult.indeterminate` (with `ok: false` — we hold no preimage), and every consumer renders `?`, not `✗`: `<SplitsPreview>`, `<LightningStatus>`, `<BoostCard>` and `BoostAllModal`'s per-track rows. **The ✗ is the bug** — it reads as "this didn't send", and re-boosting repeats *every* leg including the ones that paid. Losing sats is recoverable; sending them twice is not.
    - **It is the exact inverse of `NwcMethodUnsupportedError`, and the pair only works held together.** NOT_IMPLEMENTED is the wallet answering *instead of* paying, so it proves nothing moved and is the one error `payOne` may retry over LNURL. A reply timeout proves nothing at all, so it must never be retried — the existing retry is gated on `instanceof NwcMethodUnsupportedError`, and widening it to any timeout is a double-pay.
    - **Match the leaf class, not `Nip47TimeoutError`.** `Nip47PublishTimeoutError` shares that parent and means the opposite: the request never reached the relay, so no payment happened and an ordinary failure is the honest answer.
    - **Streaming deliberately still counts it as a failure** (`paidAny` is false, so `noteFailure` fires and two in a row stop the engine). That is the safe direction for an unattended payer, and the ledger was already debited before the await, so nothing double-spends.

**Anonymity applies to the payment, not just the Nostr note — and to BOTH sender fields.** When the share picker's **"Anonymous"** is active (`anonymous = !!identity && shareNostr && shareAs === 'site'`), every boostagram drops `sender_id` and substitutes `sender_name` — single plus boost-all per-track/host/summary legs. Each leaked the identity on its own:

- **`sender_id`** is the user's pubkey, which recipient aggregators resolve to their profile (avatar + name). Shipped first, while the note was already site-signed — the PFP leaked despite the promise. **Omitted outright.**
- **`sender_name`** is the "From" field, which recipients display verbatim **and** which `formatContent` prints into the note body as `<name> boosted N sats`. So the site-signed "anonymous" note still read *"Crimson Rook boosted 100 sats"* under the BMB npub, and `BoostAllModal`'s hand-built `contentOverride` did the same. **Replaced, not omitted** — see below.

**`DEFAULT_SENDER_NAME` (`'boostmebitch.com user'`, defined in `lib/util.ts` and re-exported by `sender-name.tsx`) is a real default, not the input's ghost text.** Both modals call `resolveSenderName(typed, anonymous)` (`lib/util.ts`), which is `(anonymous ? '' : typed.trim()) || DEFAULT_SENDER_NAME`, so it covers two cases at once: an anonymous boost, and any boost where "From" was left empty (every signed-out user who never typed one). Substituting beats omitting because `JSON.stringify` drops an `undefined` key entirely, leaving presentation to each recipient's aggregator — the same boost rendered blank in one and "Unknown" in the next. One const per modal feeds every wire site: the boostagram legs, the local `StoredBoost` (so the log matches what was sent), and `BoostAllModal`'s summary body. **Don't reintroduce a bare `name` at a wire site**, and note the summary line no longer needs a no-name branch — `senderName` is always non-empty. `formatContent`'s `'Boosted …'` fallback stays as library defense.

`<SenderName anonymous>` disables the From input, blanks it, and says the boost is sent as `DEFAULT_SENDER_NAME` — a filled-in "From" beside an anonymity promise reads as *"this is what recipients see"*. The typed value stays in parent state (not wiped), so flipping back to "My feed" restores it, and `storage.senderName` still persists the **typed** name only, never the default — anonymity is about what leaves the device.

**`anonymous` is gated on `identity`.** Signed out, the picker is a bare checkbox with no anonymous option and every note is site-signed, so the typed name is that note's *only* attribution — but `bmb:share_nostr_as` is a single global key, so a user who chose Anonymous while signed in would otherwise sign out and silently have their name replaced by the default, with no control to turn it back on. `sender_id` is unaffected by the gate (it's `identity?.pubkey`, already absent signed out). "My feed"/"Don't post" identify the sender as before.

## Streaming sats — money invariants

The engine is `lib/v4v/streaming.ts` + `lib/v4v/stream-ledger.ts`. **Off by default.** Full reasoning, including valueTimeSplits and the Split Kit live-value channel, is in [`docs/streaming.md`](docs/streaming.md) — read it before touching any of those files. These are the rules you can break from outside them:

**Every rule here follows from one property: this spends money unattended, on a timer, with no confirmation step.** A mistake has no screen in front of it to be noticed on.

- **It is not a new payment path.** Settlement calls the same `sendBoost()` with `action: 'auto'` on the boostagram. Rails, splits, TLV and the lnaddress→keysend upgrade are untouched. Keep it that way.
- **`action` is `'auto'`, not `'stream'`** — a ten-minute lump for time already listened, not a per-minute drip. `lib/v4v/boostbox.ts` must keep downgrading `'auto'` → `'stream'` for LNURL legs, whose enum rejects `'auto'` silently.
- **The ledger is debited BEFORE the payment is awaited.** Crediting after leaves the same sats for the next tick to spend again — a real double-spend. **Losing sats is recoverable; sending them twice is not.**
- **The double-pay guards live on the LEDGER, not the context.** The context is rebuilt on every reload and Fast Refresh while the balance is restored from `bmb:stream_pending` — putting them on the context charged the same track again after a refresh.
- **Settles are serialized** through a promise `chain`. Never parallelize legs or settles.
- **Elapsed time is `min(wall-clock delta, playback-position delta)`, capped at 5 min.** Wall alone bills a sleeping laptop; position alone bills a forward seek.
- **A batch below `max(10, recipientCount)` sats carries instead of sending — even when forced.**
- **Two consecutive failed settles stop streaming for that item.** A dead wallet must not accrue an unpayable debt in silence.
- **A failed settle does NOT license the claim "nothing was sent" — derive it, never infer it from the presence of an error** (`sentClause` in `components/streaming-settings.tsx`, off `StreamingStatus.lastErrorSentSats` + `lastErrorIndeterminate`). Two things make it false and neither is visible on screen. A force settle returns a `runs` **array** — turning streaming off flushes every accrued bucket in one chained batch — so bucket 1 can pay while bucket 2 fails, leaving `lastError` describing only the last one, beside sats that are already gone. And a NIP-47 reply timeout is not a refusal: the engine counts it as a failure on purpose (invariant 11's safe direction for an unattended payer), but that is a decision about what to do NEXT, not a finding about where the sats went. This is invariant 11's `?`-not-`✗` rule reaching the ambient payer — **telling someone they still have sats they have already spent is the one error re-boosting cannot undo.** Any new surface rendering a streaming failure inherits this; don't hand-roll the sentence a second time. → [`docs/streaming.md`](docs/streaming.md)
- **`stopStreamingEngine` persists but does NOT settle.** `<Player>`'s cleanup runs on every Fast Refresh — a settling teardown means *editing a comment in dev fires a real Lightning payment*.
- **No Nostr note, no confetti, no `playBoostSound`.** Streaming is ambient.
- **Logs to `bmb:streamed:<npub>`, never `bmb:boosts:*`** — that log is capped and rendered into the user's feed; six settlements an hour would evict real boosts within a day.
- **`sender_id` honors the same anonymity signal as the boost modal.** The background path must not leak a pubkey the user was told doesn't ride along.

## v4v-toolkit swap-out boundary

`lib/v4v/*` and `lib/nostr/` are intentionally the only files that talk to wallets/signers. Components reach them through a deliberately small surface — `lib/v4v/boost.ts` (orchestrator), `nwc.ts`, `spark.ts`, `webln.ts`, `wallets.ts`, `zap.ts`, `streaming.ts`, `stream-ledger.ts`, `live-value.ts`, plus the `lib/nostr/` barrel (and `lib/nostr/google-auth` directly). Swap toolkits by replacing internals here without touching `components/` or `app/`.

**The arrow only points one way, and `DEFAULT_SENDER_NAME` is where that nearly broke.** It lived in `components/boost-modal/sender-name.tsx` — reasonable, that's the "From" field — until `lib/v4v/streaming.ts` needed it and imported it from there, inverting the boundary and dragging a `'use client'` React module into the payment engine. It now lives in **`lib/util.ts`** (with `resolveSenderName`, the one place "From" becomes a wire value), and `sender-name.tsx` re-exports it so every modal import site is unchanged. `lib/util.ts` rather than `lib/v4v/` because it's a *product string*: swapping the toolkit must not delete it.

**`lib/v4v/boost.ts` importing `recipientOrder` from `lib/util.ts` is that arrow pointing the right way** — `lib/util.ts` has only a type-only import, so it drags no React or browser globals into the payment engine. **Keep `recipientOrder` there.** Moving it into `lib/v4v/` would force `components/value-split-rows.tsx` — a read-only component that touches no wallet — to import the payment engine to render a list.

**`lib/util.ts` is now where the pure money ARITHMETIC lives, and `lib/v4v/` is where money MOVES.** `recipientOrder`, `storedBoostLegs`, `splitSats`, `splitAtPosition`, `splitTrackAndHost` and `payableSplit` are all there; `boost.ts` re-exports `splitSats` so its call sites and this boundary are unchanged. The line is not stylistic — it's what makes the arithmetic **testable**: `lib/util.ts` loads under `node --experimental-strip-types` (its one import is type-only and erases), so `check:vts` imports the shipping functions rather than a copy, while nothing in `lib/v4v/` can be loaded that way. It is also what lets `<SplitsPreview>` and `<ValueSplitRows>` render an allocation without importing a wallet. **A new pure split/order/allocation helper belongs here, not in `lib/v4v/`** — put it there and it becomes unpinnable the moment it matters.

## State + persistence

Zustand store (`lib/store.ts`), **in-memory only**: `identity`, `current`, `isPlaying`, `positionSec`, `playerExpanded` (lifted so a live-stream card can open the player `<Player>` owns), `signInOpen` (lifted so the fullscreen player / live chat can open the modal `<NostrAuth>` owns), `walletOpen` (lifted so any surface can open the one `<WalletModal>` `<AuthControl>` owns), `selectedPodcast` (lifted out of `app/page.tsx` so a podcast-name link in a `<NoteCard>` can flip the layout without prop-drilling), `discussionEpisode` (cleared by `selectPodcast`), `favorites`, `favoriteEpisodes`, `favoritesSync` (one status, not one per list: there is one event now, so a partial failure isn't expressible and a single flag can't lie — `'idle'` is the pre-hydration and signed-out state, never a failure), `mutedPubkeys`, `boostsTick`, `seekReq` (`{ t, n }`, consumed by `<Player>`).

**`favorites` and `favoriteEpisodes` are the source of truth for what gets published, not a render cache.** Every id on the merged list gets a row whether or not Podcast Index resolved it; an unresolved row renders as a placeholder. Building those maps from resolved entries only meant a PI outage pruned the store while the baseline still named the un-pruned set — so the next page load read every unresolved entry as a local removal and published the deletion to a list other apps read. `addedAt: 0` means "not known yet", so a first real resolve stamps its own rather than inheriting a placeholder's and sinking to the bottom forever.

Everything else lives in `localStorage` and is never sent server-side. **All `bmb:*` keys go through typed helpers in `lib/storage.ts`** — don't call `localStorage.getItem`/`setItem` anywhere else. New persisted field ⇒ new accessor, `bmb:*` prefix.

### `safeSet` must never silently swallow a failed write

A settings control here has no local state — it writes through `storage` and renders whatever reads back. So a discarded write doesn't degrade the UI, it **freezes the control**: tap it, nothing moves, no error anywhere. That shipped, and the reported symptom was "I can't turn on streaming sats in Safari on iOS" — the switch was the most visible casualty precisely because it's a pure read-through.

Two causes, both silent and both real on iOS Safari:

- **A full store.** `setItem` throws `QuotaExceededError` for *every* subsequent write, down to a one-byte `bmb:stream_on`. Safari's per-origin quota is the tightest in the wild and this app caches whole nostr events (`bmb:feed:*` and `bmb:social:*` are unbounded in size, `bmb:profile4:*`/`bmb:pmeta:*` in count), so a long-lived install fills it. **Reads keep working**, which is why nothing else on screen looks wrong and why this is invisible in testing — a fresh profile never reproduces it.
- **A blocked store.** Private Browsing, "Block All Cookies", content blockers — `setItem` throws `SecurityError` and no amount of freeing space helps.

`safeSet` returns whether the value reached disk, and handles both:

- On a full store it **evicts one cache namespace at a time and retries**, in `EVICTABLE_PREFIXES` order (`bmb:social` → `bmb:feed` → `bmb:profile4` → `bmb:pmeta` → `bmb:epmeta`) — the note blobs first because they're nearly all the bytes, the small-but-numerous caches last because losing them costs a visible refetch. Everything on that list is network-regenerable: **no setting, no identity, no credential.** The rule is **a cache never displaces a setting, and a setting always displaces a cache** — so a failing write of an *evictable* key is left to fail rather than dropping other caches to fit one more cache.
- When the write still can't land, the value goes into `memoryMirror` and `safeGet` reads through it, so the control the user just touched works for the session. `safeRemove` deletes the mirror entry **first** — clearing only disk would let the in-memory copy resurrect a disconnected wallet or a cleared override on the next read — and `safeKeys` unions it, or a memory-held setting is invisible to the scans that count it (`showsExplicitlyOn`).
- Session-only values are surfaced, never hidden: `storage.nwcUri.isEphemeral()` and `storage.streaming.isEphemeral(showKey?)` back soft "won't survive a reload" hints. This generalizes the hand-rolled `memoryFallback.nwcUri` that used to sit in this file for the same reason.

The full `bmb:*` key table — every key, its purpose and its quirks — is in [`docs/storage.md`](docs/storage.md), along with what deliberately does *not* live in `localStorage`.

## Background art and the canvas-bg gotcha

`app/layout.tsx` renders `public/hero.jpg` as a fixed full-viewport layer with a `bg-ink/75` overlay and `<Image fill priority />`. The overlay's opacity mutes the image; in light mode `--ink` flips to cream so the same class becomes a 75% bone wash automatically. Same image doubles as the OG image.

- **The page background is set in CSS (`app/globals.css`, on `html, body`), never as a Tailwind class on `<body>`.** A background applied to `<body>` in the JSX propagates to the canvas and paints over the fixed image layer regardless of z-index — the hero breaks with no errors, just a flat-color page. The CSS rule is safe because the hero layer is a *child* of `<body>` and so paints above body's own background box.
- **`html, body` use `overflow-x: clip`, NOT `hidden`.** `hidden` computes `overflow-y` to `auto`, turning html/body into a scroll container that traps `position: sticky` descendants (the page header scrolled away instead of pinning). `clip` blocks sideways scroll without creating a scroll container. Don't switch it back.
- **Every modal renders through `<ModalShell>` (`components/modal-shell.tsx`)** — it owns the portal, `role="dialog"`/`aria-modal`/`aria-labelledby`, Escape, the focus trap and focus restore, a refcounted body-scroll lock, and the geometry below. Six hand-rolled copies had already drifted into two z-indexes, two backdrop opacities, two centring idioms and `pb-28` on four of six, and none of them had dialog semantics or a focus trap at all. Pass `dismissable={false}` while a payment is in flight — Escape and backdrop-click must not take the per-leg results off screen while legs are still settling. The rest of this bullet is the reasoning the shell now enforces once instead of six times:
- **Modals must portal to `document.body` — the layout traps `fixed` overlays.** `app/layout.tsx` wraps page content in `<div className="relative z-0">` (to sit above the fixed hero), and `<Player>` is a body-level **sibling** at `z-30`. `relative z-0` creates a **stacking context**, so a `fixed` modal inside page content is sealed in it — its `z-40`/`z-[60]` only competes *within* the wrapper and can never rise above the mini-player (symptom: the player bar paints over the modal footer). Every overlay `createPortal`s to `document.body`, guarded by a mounted `portalTarget` state so SSR renders nothing: `wallet-modal`, `sign-in-modal`, `BoostModal`, `BoostAllModal`, note-card `ZapDialog`. The `BoostModal` opened *from* `<Player>` happened to work pre-portal because it shared the player's body-level context. Modals also add `pb-28` so the centered card clears the mini-player bar.
- **A modal overlay is `fixed inset-x-0 top-0 h-[100dvh]`, never `fixed inset-0`, and its card caps at `max-h-full`, never `max-h-[92vh]`.** Two bugs stack in the obvious version. `inset-0` sizes to iOS Safari's **large** (toolbar-hidden) viewport, so the box is taller than what you can see — the same rule `<FullscreenPlayer>` already documents at its own root. And `92vh` is measured against the viewport while the card lives inside the overlay's `p-4 pb-28`, so it is up to 60px taller than the box meant to hold it *before* iOS is involved: measured at 390×844 in desktop Chrome, the card ran `-14 → 762` inside a `16 → 732` padding box. Centering then splits the overflow and pushes the header off the top, which reads as "the modal is cut off" with the body visible and the title gone. `max-h-full` resolves against the overlay's content box, so it spends exactly what the padding leaves and needs no edit if the padding changes. Applies to all six overlays (`boost-modal`, `boost-all-modal`, `wallet-modal`, `sign-in-modal`, `profile-editor`, note-card `ZapDialog`).
- **The same rule one level in: a pane INSIDE a full-height overlay is `h-full`, never `calc(100vh - <header>)`.** `<FullscreenPlayer>`'s two panes were `sm:h-[calc(100vh-3.5rem)]`, which gets the height wrong twice — the overlay is `100dvh`, and the subtracted constant is a *guess* at a header that grows with its contents (the signed-out `◆ Sign in` button is enough to break it). The panes then stand taller than the row holding them, that row is `overflow-y-auto`, and the shortfall surfaces as **a full-page scrollbar with nothing scrollable in it**. `h-full` resolves against the parent's own definite height and is right by construction. The corollary binds anything you put in such a pane: a child with no overflow of its own must fit, so size it against the space that exists (`<FullscreenPlayer>`'s video box caps its *width* by the available height — an `aspect-*` box takes its height from its width, so a `max-h` would silently break the ratio instead). → [`docs/ui.md`](docs/ui.md)

## Styling tokens

Custom palette in `tailwind.config.ts` — don't introduce new colors without adding them here: `ink` (page bg), `bone` (primary fg), `bolt` (Lightning yellow), `nostr` (magenta), `muted` (secondary), `line` (borders). Fonts: `font-display` (Bricolage Grotesque), `font-mono` (JetBrains Mono). `animate-bolt` is a 1.4 s opacity pulse.

Reusable element classes — `.card`, `.btn`, `.btn-bolt`, `.btn-ghost`, `.input`, `.stamp`, `.headline`, `.seek` — are defined in `app/globals.css`. Read that before inventing new ones.

## Conventions worth keeping

- **A read-only value split renders through `<ValueSplitRows>`** (`components/value-split-rows.tsx`) — the show header, the episode detail page and the fullscreen player's disclosure all use it, ordered by `recipientOrder` (biggest share first; feed order is authoring order and buries the recipient that matters). It was three copies, and they had already drifted: `lists.tsx` carried its own inlined address elision instead of `recipientAddress`, which is exactly what that helper's comment forbids, so the same pubkey rendered differently depending on the screen. **`<SplitsPreview>` in the boost modal is deliberately separate** — it carries per-leg sats and ✓/✗ delivery status against a live payment, and sharing would mean one component branching on a flag.
- **`recipientOrder(recipients)` (`lib/util.ts`) returns INDICES, not recipients.** `<SplitsPreview>` reads `splits[i]` and `results[i]` positionally, so a reordered array would pair each row with someone else's sats and someone else's ✓/✗ — a display bug indistinguishable from a payment bug. The sort is stable, so equal weights keep feed order. **It is no longer display-only:** `sendBoost` traverses by it (invariant 10) and `storedBoostLegs` writes the permanent history record by it, so this one function decides both the payment sequence and how a boost is remembered — and its stability guarantee is now a payment-determinism guarantee. Don't give it fee-awareness or an opt-out: it's shared with `<ValueSplitRows>`, so any semantic change silently restyles the show header, the episode page and the fullscreen player.
- **A sent boost's `StoredBoost.legs` are built by `storedBoostLegs(results)` (`lib/util.ts`)** — three sites hand-rolled the identical `results.map(…)` (`boost-modal/index.tsx`, and twice in `boost-all-modal.tsx`). It orders biggest-share-first because `<BoostCard>` renders `legs` verbatim and a stored leg carries **no `split` weight**, so whatever order is written is permanent and can't be re-sorted at render time — feed order meant the history card listed a payment differently from the modal that sent it, the same complaint as invariant 10 one screen removed. Order derives from `results` alone (`results[i].recipient` *is* `recipients[i]`), so there's no second array to pass out of sync. Boosts stored before this keep feed order; **deliberately not migrated**, since rewriting a user's local money log for cosmetics is a bad trade.
- **`isMusicMedium(podcast)` and `hasValueRecipients(value)` live in `lib/util.ts`** (isomorphic, type-only imports). Use them instead of inlining `podcast.medium?.toLowerCase() === 'music'` or `!!value?.recipients?.length`. `hasValueRecipients` returns a boolean and does **not** narrow its argument — at a site that then uses the value as non-null, assert `value!` after the guard. These are the **boolean gate** helpers; expressions rendering the recipient *count* stay inline.
- **`isHlsUrl(url)` and `fnvHash(s)` also live in `lib/util.ts`.** `isHlsUrl` (matches `.m3u8`) branches the HLS-video path from native `<audio>`. `fnvHash` is the FNV-1a → stable 31-bit int for deterministic numeric IDs; one copy, imported by `lib/pi.ts` and `lib/nostr/live-streams.ts`.
- **Transport buttons go through `<TransportControls size>`** (`components/transport-controls.tsx`) — never re-inline ⏮/play/⏭. It reads playback + `episodeQueue` from the store, computes prev/next-disabled, and renders as a fragment into the parent's flex row.
- **Live-stream id parsing goes through `lib/nostr/live-streams.ts` helpers** — `streamIdOf` / `parseStreamId` / `isLiveStreamId`, and `streamChatAddr` in live-chat.ts.
- **Time-of-day formatters live in `lib/format.tsx`:** `fmtClock(unixSec)` and `fmtLiveTime(unixSec)` (clock for today, else "Mon D <clock>"). `fmt`/`fmtDuration` are for playback *durations*, `timeAgo` for relative. Don't re-inline `toLocaleTimeString`.
- **`NoteCard` is memoized** (`memo(NoteCardImpl)`). Feed surfaces re-render wholesale (podcast metadata resolving, `boostsTick`) and note refs are stable, so memo skips untouched cards. Two rules keep it correct: `repostedIds` must be **replaced, not mutated in place**, and store-driven values (identity, mutes) stay read via `useApp` selectors *inside* the component so they bypass memo.
- **Horizontal card rows use `useHorizontalWheelScroll()`** (`lib/use-horizontal-wheel.ts`) — a mouse has no sideways wheel, so without it the off-screen cards are unreachable. React's `onWheel` is passive, so the listener attaches natively, and it hijacks the gesture only when the row overflows, the gesture is vertical, and it isn't at the edge, so page scroll still takes over at the end. **It returns a callback ref** — `const rowRef = useHorizontalWheelScroll<HTMLDivElement>()` then `<div ref={rowRef}>` — and that's load-bearing: both consumers render a skeleton with no ref until data resolves, so a `useEffect` reading `ref.current` sees null, bails, and never re-runs unless its dep array happens to change when the row mounts. The original leaned on `[podcasts.length]`/`[resolved.length]` for exactly that, which reads like an unrelated perf dep and got "simplified" away once, silently killing the wheel on both rows. A callback ref has no dependency to get wrong.
- **`<FavHeart>` and `<FavEpisodeHeart>` live in `components/fav-heart.tsx`**, not `lists.tsx` — several surfaces render them (`lists.tsx`, `fullscreen-player.tsx`, `podroll.tsx`, `episode-detail-view.tsx`), and having `podroll.tsx` import from `lists.tsx` while `lists.tsx` imports `<Podroll>` made a module cycle. `lists.tsx` re-exports both. **`<FavEpisodeHeart>` renders nothing without BOTH an item guid and a parent feed guid** — an episode favorite with no parent feed is unresolvable on any other device (PI's `/episodes/byguid` needs `podcastguid`), so offering it would create a favorite that syncs but can never be opened. It calls `stopPropagation()`/`preventDefault()` itself, so it's safe inside a clickable row. **`size` variants:** `'sm'` (default, list rows and podroll cards) is a slim border-chip; `'md'` (show header) matches `.btn-ghost` so it's a peer to SHARE and BOOST. Both render `[♡ FAVORITE]` / `[♥ FAVORITED]` (magenta when on) — don't reintroduce the old bare-glyph `♡` without rethinking the header cluster. **`nameTarget` swaps that word for the TARGET — `[♡ SHOW]` / `[♡ EPISODE]`, or `ALBUM`/`TRACK` on a music feed — and belongs anywhere both hearts share one cluster.** The fullscreen player is the only such place today, and it shipped rendering the identical word twice side by side with nothing on screen saying which favorited what. The filled magenta heart already carries favorited-ness, so the word is free to carry the target; the `aria-label` spells out the whole action either way and does not change. Keep the vocabulary in `targetWord` rather than passing strings in, or two surfaces will disagree about whether an item is an "episode" or a "track".
- **A per-track control binds to a `<podcast:valueTimeSplit>` window, never to a chapter — and the two now render in ONE list, which is exactly why this rule needs restating.** Tracks and chapters used to be separate tabs; they are merged into `<EpisodeContents>` (`components/episode-contents.tsx`) because two near-identical lists naming the same songs was a UI the listener had to decode. **The merge is by timestamp alone and `mergeEpisodeContents` (`lib/util.ts`) never maps a chapter to a window**: a track row carries its window, a chapter row carries no identifiers, and they are distinct variants of `EpisodeContentRow` so a chapter row has no `split` to hand `<FavTrackHeart>` even by accident. The rows now sit next to each other, so the tempting edit — "put a heart on the chapter row too" — looks harmless and is the bug. One is not derivable from the other: on *Mutton, Mead & Music* 150 two chapters tie on a `28:20` start with the tie resolving to a talk break, and on **Homegrown Hits 146 the windows OVERLAP** (`1919 + 285 = 2204`, past the `2192` window's start), so "the window covering this chapter" gives the *Cloud Burst* chapter *Victim [432Hz]*'s identifiers **even though Cloud Burst has its own window at its own start** — three songs on that one episode, each favoritable as the wrong track, silently. The window is also the only one of the two carrying a `feedGuid`/`itemGuid`, which is what a favorite records. Two rules the merge itself must keep, both pinned by `check:vts` against the real wire: **only a chapter may ever be dropped** (dropping a window removes a heart invisibly), and **dedupe on a tolerance, not equality** — PI returns integer `startTime`s against a fractional chapters JSON, so 13 of HGH 146's 14 pairs differ by a fraction of a second and exact matching leaves duplicate rows. **Say "this episode isn't playing" with `currentSec={undefined}`, never `0`** — `splitAtPosition`'s interval is half-open `[start, …)`, so 0 matches any window authored at `startTime: 0`, which is the norm on a playlist opening straight on a track; passing 0 as a sentinel lit the first row as the playing track on a show nobody had started. 0 is a real position and cannot also mean "inactive". → [`docs/ui.md`](docs/ui.md)
- **A chapter/track row's thumbnail goes through `<RowThumb>` (`components/chapter-ui.tsx`)** — never a hand-rolled `<img>` with an `onError` fallback. That handler terminates on a `data-fell-back` marker rather than a string compare because `HTMLImageElement.src`'s *getter* returns a resolved absolute URL while the fallback is a raw feed string, so a relative or untrimmed URL never compares equal and re-assigns the same failing URL **forever** — a tight loop against an ad-blocked host, in an always-mounted component. Three lists need it and it had already been copied twice. → [`docs/ui.md`](docs/ui.md)
- **Profile avatars go through `<Avatar>`** (`components/avatar.tsx`) — `<img src={picture}>` with a deterministic colored-initial `<DefaultAvatar>` fallback on error/missing. For *user* avatars, distinct from `<PodcastCover>`.
- **Podcast artwork goes through `<PodcastCover>`** (`components/podcast-cover.tsx`). Tries `image`, falls back to `artwork` on `onError`, then a colored-initial tile. The two-URL fallback exists because PI returns RSS `<image><url>` as `image` and `<itunes:image>` as `artwork`, and they often disagree (Homegrown Hits has a dead `image` but a working `artwork`). Always pass both. Uses `<img>`, not `next/image` (arbitrary hosts); the local hero IS served via `next/image`.
- **Auxiliary relay sets use `withExtraRelays`** (`lib/nostr/pool.ts`) — it dedupes the union, runs the query in the closure, and closes only newly-opened extras in `finally`. Don't write the open/track/try-finally/close pattern inline; four near-identical copies were collapsed.
- **Browse-mode layout is single-column** in `app/page.tsx`. Selecting a podcast sets `selectedPodcast`, flipping to detail view; `discussionEpisode` adds a third full-page view. Both are state-driven swaps, not routes. Don't reintroduce a right-pane "select a podcast on the left" empty state.
- Native HTML5 `<audio>` plays the enclosure URL directly — no proxy, no transcoding. The one exception is HLS live streams.
- **API routes** return `{ error }` JSON via `getErrorMessage(e, fallback)`; clients swallow errors silently. New routes start with `rateLimit(req, '<route>', N)` (per-IP, 60 s window; by-guid runs at 300/min to absorb the favorites-hydration burst) and set `Cache-Control` on **200 responses only**.
- **A route that fans out to Podcast Index must cap the fan-out, and the cap belongs on any list that came from a feed.** `/api/publisher` used to `Promise.all` `getPodcastByFeedUrl` over *every* album URL in a third-party publisher feed — an attacker-chosen list length turning one cheap request into N parallel PI calls, a request-amplification lever aimed at our own PI quota. It now slices to `MAX_PUBLISHER_ALBUMS` and follows **probe-first-then-batch**. The probe is deliberately **uncaught** — `getPodcastByFeedUrl` already turns PI's 400/404 into `null`, so a throw means PI itself is down and the resulting 5xx is exactly what should trip the client-side breaker.
- **Inline SVG `BoltIcon`** (`components/icons.tsx`) on yellow buttons — the `⚡` emoji is invisible on `bg-bolt`. Yellow-text-on-dark and V4V stamps keep the emoji. `ShareIcon` and `Sun`/`MoonIcon` follow the same inherits-`currentColor` pattern.
